# CNPJ no cadastro + login por CNPJ — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tornar o CNPJ obrigatório no cadastro (com validação de dígitos + consulta BrasilAPI) e permitir login com email **ou** CNPJ, guardando dados fiscais em uma tabela `TenantFiscal` 1:1.

**Architecture:** `cnpj` normalizado e único no `Tenant` (login rápido); dados enriquecidos em `TenantFiscal`. Um `cnpj.service` puro faz validação offline + lookup externo server-side. Registro e `authorize` do NextAuth passam a usar CNPJ. Tenants antigos têm gate de carência de 14 dias.

**Tech Stack:** Next.js 14, TypeScript, Prisma 7, NextAuth (Credentials), Zod, Vitest 4, BrasilAPI/ReceitaWS.

---

## Mapa de arquivos

| Arquivo | Ação |
|---|---|
| `prisma/schema.prisma` | `cnpj` no `Tenant` + modelo `TenantFiscal` + migration |
| `src/services/fiscal/cnpj.service.ts` | criar (validação + lookup) |
| `src/services/fiscal/__tests__/cnpj.service.test.ts` | criar |
| `src/lib/validations.ts` | `cnpj` no `registerSchema` |
| `src/app/api/auth/register/route.ts` | validar unicidade + lookup + criar `TenantFiscal` |
| `src/lib/auth.ts` | `authorize` aceita email ou CNPJ |
| `src/app/auth/cadastro/page.tsx` | campo CNPJ com máscara |
| `src/app/(dashboard)/configuracoes/restaurante/page.tsx` | preencher CNPJ pós-cadastro |
| `src/app/(dashboard)/layout.tsx` (ou equivalente) | gate de carência (banner/modal) |

---

## Task 1: Schema — `cnpj` no Tenant + `TenantFiscal`

**Files:**
- Modify: `prisma/schema.prisma`

- [ ] **Step 1: Adicionar campo `cnpj` e relação no `Tenant`**

No `model Tenant`, junto aos campos escalares (após `phone`):

```prisma
cnpj                String?              @unique
```

E na seção de relações do `Tenant` (junto às outras relações, ex. perto de `nfsProcessadas`):

```prisma
fiscal              TenantFiscal?
```

- [ ] **Step 2: Adicionar o modelo `TenantFiscal`**

No final do bloco de modelos relacionados a Tenant (ex.: logo após o `model Supplier` ou perto dos modelos fiscais):

```prisma
model TenantFiscal {
  id                String   @id @default(cuid())
  tenantId          String   @unique
  tenant            Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  razaoSocial       String?
  nomeFantasia      String?
  situacaoCadastral String?
  cnpjVerifiedAt    DateTime?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
}
```

- [ ] **Step 3: Gerar a migration**

```bash
npx prisma migrate dev --name add_cnpj_and_tenant_fiscal
```
Esperado: migration criada em `prisma/migrations/...`, client regenerado, 0 erros. (Coluna `cnpj` é nullable → não quebra tenants existentes.)

- [ ] **Step 4: Verificar tipos**

```bash
npx tsc --noEmit
```
Esperado: 0 erros.

- [ ] **Step 5: Commit**

```bash
git add prisma/schema.prisma prisma/migrations
git commit -m "feat(db): add cnpj to Tenant and TenantFiscal model"
```

---

## Task 2: `cnpj.service` — validação offline (TDD)

**Files:**
- Create: `src/services/fiscal/cnpj.service.ts`
- Create: `src/services/fiscal/__tests__/cnpj.service.test.ts`

- [ ] **Step 1: Escrever o teste falhando**

Crie `src/services/fiscal/__tests__/cnpj.service.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { normalizeCnpj, formatCnpj, isValidCnpj } from '../cnpj.service'

describe('normalizeCnpj', () => {
  it('remove máscara deixando só dígitos', () => {
    expect(normalizeCnpj('11.222.333/0001-81')).toBe('11222333000181')
  })
})

describe('formatCnpj', () => {
  it('formata 14 dígitos com máscara', () => {
    expect(formatCnpj('11222333000181')).toBe('11.222.333/0001-81')
  })
})

describe('isValidCnpj', () => {
  it('aceita um CNPJ válido', () => {
    expect(isValidCnpj('11.222.333/0001-81')).toBe(true)
  })
  it('rejeita dígitos verificadores errados', () => {
    expect(isValidCnpj('11.222.333/0001-00')).toBe(false)
  })
  it('rejeita tamanho errado', () => {
    expect(isValidCnpj('1122233300018')).toBe(false)
  })
  it('rejeita todos os dígitos iguais', () => {
    expect(isValidCnpj('11111111111111')).toBe(false)
  })
})
```

- [ ] **Step 2: Rodar e confirmar falha**

```bash
npx vitest run src/services/fiscal/__tests__/cnpj.service.test.ts
```
Esperado: FAIL — módulo não existe.

- [ ] **Step 3: Implementar as funções puras**

Crie `src/services/fiscal/cnpj.service.ts`:

```typescript
export function normalizeCnpj(raw: string): string {
  return (raw ?? '').replace(/\D/g, '')
}

export function formatCnpj(digits: string): string {
  const d = normalizeCnpj(digits)
  if (d.length !== 14) return digits
  return `${d.slice(0, 2)}.${d.slice(2, 5)}.${d.slice(5, 8)}/${d.slice(8, 12)}-${d.slice(12)}`
}

function calcCheckDigit(base: string): number {
  const weights = base.length === 12
    ? [5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2]
    : [6, 5, 4, 3, 2, 9, 8, 7, 6, 5, 4, 3, 2]
  const sum = base
    .split('')
    .reduce((acc, ch, i) => acc + Number(ch) * weights[i], 0)
  const rest = sum % 11
  return rest < 2 ? 0 : 11 - rest
}

export function isValidCnpj(raw: string): boolean {
  const d = normalizeCnpj(raw)
  if (d.length !== 14) return false
  if (/^(\d)\1{13}$/.test(d)) return false // todos iguais
  const dv1 = calcCheckDigit(d.slice(0, 12))
  const dv2 = calcCheckDigit(d.slice(0, 12) + dv1)
  return d.slice(12) === `${dv1}${dv2}`
}
```

- [ ] **Step 4: Rodar e confirmar verde**

```bash
npx vitest run src/services/fiscal/__tests__/cnpj.service.test.ts
```
Esperado: todos PASS.

- [ ] **Step 5: Commit**

```bash
git add src/services/fiscal/cnpj.service.ts src/services/fiscal/__tests__/cnpj.service.test.ts
git commit -m "feat(fiscal): cnpj offline validation (normalize/format/isValid)"
```

---

## Task 3: `cnpj.service` — lookup BrasilAPI (TDD)

**Files:**
- Modify: `src/services/fiscal/cnpj.service.ts`
- Modify: `src/services/fiscal/__tests__/cnpj.service.test.ts`

- [ ] **Step 1: Adicionar testes de `lookupCnpj` com `fetch` mockado**

Acrescente ao arquivo de teste:

```typescript
import { vi, beforeEach } from 'vitest'
import { lookupCnpj } from '../cnpj.service'

describe('lookupCnpj', () => {
  beforeEach(() => { vi.restoreAllMocks() })

  it('retorna ok + dados quando BrasilAPI acha ativo', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
      ok: true,
      json: async () => ({ razao_social: 'ACME LTDA', nome_fantasia: 'ACME', descricao_situacao_cadastral: 'ATIVA' }),
    }))
    const r = await lookupCnpj('11222333000181')
    expect(r.status).toBe('ok')
    if (r.status === 'ok') {
      expect(r.data.razaoSocial).toBe('ACME LTDA')
      expect(r.data.ativo).toBe(true)
    }
  })

  it('retorna inactive quando situação não é ATIVA', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
      ok: true,
      json: async () => ({ razao_social: 'X', descricao_situacao_cadastral: 'BAIXADA' }),
    }))
    const r = await lookupCnpj('11222333000181')
    expect(r.status).toBe('inactive')
  })

  it('retorna not_found em 404', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({ ok: false, status: 404 }))
    const r = await lookupCnpj('11222333000181')
    expect(r.status).toBe('not_found')
  })

  it('retorna unavailable quando fetch lança (API fora do ar)', async () => {
    vi.stubGlobal('fetch', vi.fn().mockRejectedValue(new Error('network')))
    const r = await lookupCnpj('11222333000181')
    expect(r.status).toBe('unavailable')
  })
})
```

- [ ] **Step 2: Rodar e confirmar falha**

```bash
npx vitest run src/services/fiscal/__tests__/cnpj.service.test.ts
```
Esperado: FAIL — `lookupCnpj` não existe.

- [ ] **Step 3: Implementar `lookupCnpj`**

Acrescente a `src/services/fiscal/cnpj.service.ts`:

```typescript
export type CnpjLookup = {
  razaoSocial: string | null
  nomeFantasia: string | null
  situacaoCadastral: string | null
  ativo: boolean
}
export type CnpjLookupResult =
  | { status: 'ok'; data: CnpjLookup }
  | { status: 'inactive'; data: CnpjLookup }
  | { status: 'not_found' }
  | { status: 'unavailable' }

async function fetchJson(url: string, timeoutMs = 5000): Promise<unknown | null> {
  const ctrl = new AbortController()
  const t = setTimeout(() => ctrl.abort(), timeoutMs)
  try {
    const res = await fetch(url, { signal: ctrl.signal })
    if (res.status === 404) return { __notFound: true }
    if (!res.ok) return null
    return await res.json()
  } catch {
    return null
  } finally {
    clearTimeout(t)
  }
}

export async function lookupCnpj(digits: string): Promise<CnpjLookupResult> {
  const cnpj = normalizeCnpj(digits)
  // BrasilAPI primeiro
  let raw = await fetchJson(`https://brasilapi.com.br/api/cnpj/v1/${cnpj}`)
  if (raw && (raw as { __notFound?: boolean }).__notFound) return { status: 'not_found' }
  // fallback ReceitaWS
  if (!raw) {
    raw = await fetchJson(`https://www.receitaws.com.br/v1/cnpj/${cnpj}`)
    if (raw && (raw as { __notFound?: boolean }).__notFound) return { status: 'not_found' }
  }
  if (!raw) return { status: 'unavailable' }

  const o = raw as Record<string, unknown>
  const situacao = String(
    o.descricao_situacao_cadastral ?? o.situacao ?? ''
  ).toUpperCase()
  const ativo = situacao.includes('ATIVA')
  const data: CnpjLookup = {
    razaoSocial: (o.razao_social ?? o.nome ?? null) as string | null,
    nomeFantasia: (o.nome_fantasia ?? o.fantasia ?? null) as string | null,
    situacaoCadastral: situacao || null,
    ativo,
  }
  return { status: ativo ? 'ok' : 'inactive', data }
}
```

- [ ] **Step 4: Rodar e confirmar verde**

```bash
npx vitest run src/services/fiscal/__tests__/cnpj.service.test.ts
```
Esperado: todos PASS.

- [ ] **Step 5: Commit**

```bash
git add src/services/fiscal/cnpj.service.ts src/services/fiscal/__tests__/cnpj.service.test.ts
git commit -m "feat(fiscal): cnpj lookup via BrasilAPI with ReceitaWS fallback"
```

---

## Task 4: registerSchema + rota de registro

**Files:**
- Modify: `src/lib/validations.ts`
- Modify: `src/app/api/auth/register/route.ts`

- [ ] **Step 1: Adicionar `cnpj` ao `registerSchema`**

Em `src/lib/validations.ts`, importe o validador e adicione o campo:

```typescript
import { isValidCnpj } from '@/services/fiscal/cnpj.service'
```

No `registerSchema`, após `password`:

```typescript
  cnpj: z
    .string()
    .min(1, 'CNPJ é obrigatório')
    .refine(isValidCnpj, 'CNPJ inválido'),
```

- [ ] **Step 2: Atualizar a rota de registro**

Em `src/app/api/auth/register/route.ts`:

Imports no topo:
```typescript
import { normalizeCnpj, lookupCnpj } from '@/services/fiscal/cnpj.service'
```

Após `const { restaurantName, name, email, password } = parsed.data`, troque por incluir o cnpj e adicione a lógica antes do `prisma.tenant.create`:

```typescript
    const { restaurantName, name, email, password, cnpj } = parsed.data
    const cnpjDigits = normalizeCnpj(cnpj)

    // unicidade (mensagem genérica — não revela existência)
    const cnpjEmUso = await prisma.tenant.findUnique({ where: { cnpj: cnpjDigits } })
    if (cnpjEmUso) {
      return NextResponse.json(
        { error: 'Não foi possível criar a conta. Verifique os dados.' },
        { status: 400 }
      )
    }

    // lookup: bloqueia inativo/inexistente; API fora do ar não bloqueia
    const lookup = await lookupCnpj(cnpjDigits)
    if (lookup.status === 'not_found' || lookup.status === 'inactive') {
      return NextResponse.json(
        { error: 'CNPJ não encontrado ou inativo na Receita Federal.' },
        { status: 400 }
      )
    }
    const fiscalData = lookup.status === 'ok'
      ? { razaoSocial: lookup.data.razaoSocial, nomeFantasia: lookup.data.nomeFantasia, situacaoCadastral: lookup.data.situacaoCadastral, cnpjVerifiedAt: new Date() }
      : { cnpjVerifiedAt: null }
```

E no `prisma.tenant.create`, adicione `cnpj` e crie o `fiscal`:

```typescript
    const tenant = await prisma.tenant.create({
      data: {
        name: restaurantName,
        slug,
        cnpj: cnpjDigits,
        fiscal: { create: fiscalData },
        users: {
          create: { name, email, password: hashedPassword, role: 'ADMIN' },
        },
      },
    })
```

- [ ] **Step 3: Verificar tipos + testes**

```bash
npx tsc --noEmit
npm test
```
Esperado: 0 erros; suíte verde.

- [ ] **Step 4: Commit**

```bash
git add src/lib/validations.ts src/app/api/auth/register/route.ts
git commit -m "feat(auth): require valid CNPJ on registration + persist TenantFiscal"
```

---

## Task 5: `authorize` aceita email ou CNPJ

**Files:**
- Modify: `src/lib/auth.ts`

- [ ] **Step 1: Importar o serviço**

No topo de `src/lib/auth.ts`:
```typescript
import { isValidCnpj, normalizeCnpj } from '@/services/fiscal/cnpj.service'
```

- [ ] **Step 2: Resolver o usuário por email OU CNPJ**

No `authorize`, substitua o bloco que faz `prisma.user.findUnique({ where: { email: ... } })` por uma resolução condicional. O campo de credencial continua chamado `email`, mas é tratado como identificador:

```typescript
        const identifier = credentials.email.trim()
        const include = {
          tenant: { include: { subscription: { include: { plan: true } } } },
          customRole: true,
        }

        let user
        if (isValidCnpj(identifier)) {
          const tenant = await prisma.tenant.findUnique({
            where: { cnpj: normalizeCnpj(identifier) },
            select: { id: true },
          })
          user = tenant
            ? await prisma.user.findFirst({
                where: { tenantId: tenant.id, role: 'ADMIN' },
                include,
              })
            : null
        } else {
          user = await prisma.user.findUnique({
            where: { email: identifier.toLowerCase() },
            include,
          })
        }
```

> O restante do fluxo (dummy bcrypt timing-safe, lockout, mensagens genéricas, reset de tentativas) permanece **inalterado** operando sobre `user`.

- [ ] **Step 3: Verificar tipos + testes**

```bash
npx tsc --noEmit
npm test
```
Esperado: 0 erros; suíte verde.

- [ ] **Step 4: Commit**

```bash
git add src/lib/auth.ts
git commit -m "feat(auth): allow login with email or CNPJ (resolves to admin)"
```

---

## Task 6: Campo CNPJ no cadastro

**Files:**
- Modify: `src/app/auth/cadastro/page.tsx`

- [ ] **Step 1: Ler a estrutura atual do formulário**

Abra `src/app/auth/cadastro/page.tsx` e localize o `formData` (estado) e os inputs do passo `form` (restaurantName, name, email, password).

- [ ] **Step 2: Adicionar CNPJ ao estado e um input com máscara**

Adicione `cnpj: ''` ao `formData`. Crie um handler de máscara e um input seguindo o **mesmo padrão visual dos outros campos** da página:

```tsx
function maskCnpj(v: string) {
  return v.replace(/\D/g, '').slice(0, 14)
    .replace(/^(\d{2})(\d)/, '$1.$2')
    .replace(/^(\d{2})\.(\d{3})(\d)/, '$1.$2.$3')
    .replace(/\.(\d{3})(\d)/, '.$1/$2')
    .replace(/(\d{4})(\d)/, '$1-$2')
}
```

Input (mesmo estilo dos demais campos da página, label "CNPJ", `value={formData.cnpj}`, `onChange={e => setFormData({ ...formData, cnpj: maskCnpj(e.target.value) })}`, `inputMode="numeric"`, obrigatório).

- [ ] **Step 3: Incluir `cnpj` no corpo enviado ao registro**

No fetch que chama `/api/auth/register`, inclua `cnpj: formData.cnpj` no body. Trate erro 400 de CNPJ exibindo a mensagem retornada (toast/inline, como os outros erros da página).

- [ ] **Step 4: Verificar tipos + lint**

```bash
npx tsc --noEmit
npx next lint --file "src/app/auth/cadastro/page.tsx"
```
Esperado: 0 erros/warnings.

- [ ] **Step 5: Commit**

```bash
git add src/app/auth/cadastro/page.tsx
git commit -m "feat(auth): CNPJ field on registration page"
```

---

## Task 7: Preencher CNPJ em Configurações › Restaurante

**Files:**
- Modify: `src/app/(dashboard)/configuracoes/restaurante/page.tsx`
- Create: `src/app/api/configuracoes/cnpj/route.ts`

- [ ] **Step 1: Criar a rota que grava o CNPJ pós-cadastro**

Crie `src/app/api/configuracoes/cnpj/route.ts`:

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { getSession, unauthorizedResponse } from '@/lib/session'
import { prisma } from '@/lib/prisma'
import { isValidCnpj, normalizeCnpj, lookupCnpj } from '@/services/fiscal/cnpj.service'

export async function POST(req: NextRequest) {
  const session = await getSession()
  if (!session?.user?.tenantId) return unauthorizedResponse()
  const isAdmin = session.user.role === 'ADMIN' || session.user.role === 'SUPER_ADMIN'
  if (!isAdmin) return NextResponse.json({ error: 'Sem permissão' }, { status: 403 })

  const { cnpj } = await req.json()
  if (!isValidCnpj(cnpj)) return NextResponse.json({ error: 'CNPJ inválido' }, { status: 400 })
  const digits = normalizeCnpj(cnpj)

  const emUso = await prisma.tenant.findFirst({ where: { cnpj: digits, NOT: { id: session.user.tenantId } } })
  if (emUso) return NextResponse.json({ error: 'CNPJ já cadastrado em outra conta.' }, { status: 400 })

  const lookup = await lookupCnpj(digits)
  if (lookup.status === 'not_found' || lookup.status === 'inactive') {
    return NextResponse.json({ error: 'CNPJ não encontrado ou inativo na Receita Federal.' }, { status: 400 })
  }
  const fiscalData = lookup.status === 'ok'
    ? { razaoSocial: lookup.data.razaoSocial, nomeFantasia: lookup.data.nomeFantasia, situacaoCadastral: lookup.data.situacaoCadastral, cnpjVerifiedAt: new Date() }
    : { cnpjVerifiedAt: null }

  await prisma.tenant.update({
    where: { id: session.user.tenantId },
    data: {
      cnpj: digits,
      fiscal: { upsert: { create: fiscalData, update: fiscalData } },
    },
  })
  return NextResponse.json({ ok: true })
}
```

- [ ] **Step 2: Adicionar o campo CNPJ na página de restaurante**

Em `src/app/(dashboard)/configuracoes/restaurante/page.tsx`, adicione um campo CNPJ (mesma máscara `maskCnpj` da Task 6, seguindo o padrão `--tf-*` da página) que chama `POST /api/configuracoes/cnpj`. Se o tenant já tem CNPJ, exibir formatado e somente-leitura.

- [ ] **Step 3: Verificar tipos + lint + testes**

```bash
npx tsc --noEmit
npx next lint --file "src/app/(dashboard)/configuracoes/restaurante/page.tsx"
npm test
```
Esperado: 0 erros; suíte verde.

- [ ] **Step 4: Commit**

```bash
git add "src/app/(dashboard)/configuracoes/restaurante/page.tsx" src/app/api/configuracoes/cnpj/route.ts
git commit -m "feat(settings): allow filling CNPJ after signup"
```

---

## Task 8: Gate de carência (banner/modal)

**Files:**
- Modify: `src/app/api/assinatura/route.ts` ou a fonte de dados do tenant (expor `cnpj` + `createdAt`)
- Create: `src/components/CnpjGate.tsx`
- Modify: layout do dashboard (`src/app/(dashboard)/layout.tsx`)

- [ ] **Step 1: Expor se o tenant tem CNPJ e há quantos dias foi criado**

Garanta que algum endpoint já consumido pelo dashboard (ex.: `/api/assinatura` ou um `/api/tenant/me`) retorne `{ cnpj: string | null, createdAt: string }` do tenant logado. Se não existir, crie `src/app/api/tenant/me/route.ts`:

```typescript
import { NextResponse } from 'next/server'
import { getSession, unauthorizedResponse } from '@/lib/session'
import { prisma } from '@/lib/prisma'

export async function GET() {
  const session = await getSession()
  if (!session?.user?.tenantId) return unauthorizedResponse()
  const t = await prisma.tenant.findUnique({
    where: { id: session.user.tenantId },
    select: { cnpj: true, createdAt: true },
  })
  return NextResponse.json(t)
}
```

- [ ] **Step 2: Criar o componente `CnpjGate`**

Crie `src/components/CnpjGate.tsx` (client). Busca `/api/tenant/me`; se `cnpj` for null:
- calcula dias desde `createdAt`;
- `<= 14 dias`: renderiza um banner persistente (estilo `--tf-yellow*`) com link para Configurações › Restaurante e um modal mostrado 1x por sessão (controle via `sessionStorage`);
- `> 14 dias`: renderiza um modal **não dispensável** (sem botão de fechar) com o mesmo CTA.

Use os tokens `--tf-*` (ver `project-theme-css-vars`). Sem hard-block de rota — apenas overlay no client.

- [ ] **Step 3: Montar o gate no layout do dashboard**

No layout do dashboard, renderize `<CnpjGate />` junto ao conteúdo (após o provider de sessão), para aparecer em todas as telas autenticadas.

- [ ] **Step 4: Verificar tipos + lint**

```bash
npx tsc --noEmit
npx next lint
```
Esperado: 0 erros/warnings.

- [ ] **Step 5: Commit**

```bash
git add src/components/CnpjGate.tsx "src/app/(dashboard)/layout.tsx" src/app/api/tenant/me/route.ts
git commit -m "feat(auth): grace-period CNPJ gate for existing tenants"
```

---

## Task 9: Verificação final (testes + segurança)

**Files:** nenhum (verificação)

- [ ] **Step 1: Suíte completa + tipos + lint**

```bash
npm test
npx tsc --noEmit
npx next lint
```
Esperado: tudo verde, 0 erros.

- [ ] **Step 2: Auditoria de dependências**

```bash
npm audit --omit=dev
```
Esperado: revisar; nenhuma vulnerabilidade nova introduzida por este trabalho (não adicionamos dependências — usamos `fetch` nativo).

- [ ] **Step 3: Checklist de vulnerabilidades (revisão manual)**

Confirmar no código:
- Login com CNPJ inexistente retorna **a mesma** mensagem genérica que email inválido (sem oracle de enumeração).
- Rate-limit ativo nas rotas de auth/registro (já existente via `rateLimit`).
- `cnpj` sempre normalizado (só dígitos) antes de qualquer query ou montagem de URL (sem SSRF/injeção).
- `@unique` no `cnpj` garante unicidade no banco (não só no app).
- `TenantFiscal` guarda o mínimo de PII; anotar gancho para o sub-projeto **G/LGPD**.

- [ ] **Step 4: Commit (se algum ajuste de segurança for necessário)**

```bash
git add -A
git commit -m "chore(auth): security review fixes for CNPJ flow"
```

---

## Self-Review

**Cobertura do spec:**
- [x] Modelo de dados (`cnpj` no Tenant + `TenantFiscal`) → Task 1
- [x] Serviço de CNPJ (validação offline) → Task 2
- [x] Serviço de CNPJ (lookup BrasilAPI/ReceitaWS, regras ok/inactive/not_found/unavailable) → Task 3
- [x] Cadastro obrigatório (schema + rota) → Tasks 4, 6
- [x] Login email ou CNPJ → Task 5
- [x] Preenchimento pós-cadastro (Configurações) → Task 7
- [x] Gate de carência 14 dias → Task 8
- [x] Segurança/testes/vulnerabilidade → Tasks 2–9 + Task 9

**Placeholders:** Nenhum nas partes testáveis (schema/serviço/rota/authorize têm código completo). Tasks 6–8 referenciam o padrão de UI existente por serem edições em arquivos grandes — integração descrita com pontos exatos.

**Consistência de tipos:** `normalizeCnpj`/`isValidCnpj`/`lookupCnpj` + `CnpjLookupResult` (status `ok|inactive|not_found|unavailable`) usados de forma idêntica em register route, settings route e authorize. `fiscalData` tem a mesma forma nas Tasks 4 e 7.
