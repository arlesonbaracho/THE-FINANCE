# LGPD Transparência & Consentimento (G2a) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) ou superpowers:executing-plans para implementar tarefa-a-tarefa. Steps usam checkbox (`- [ ]`).

**Goal:** Publicar Política de Privacidade/Termos (minuta LGPD), capturar e registrar consentimento no cadastro e no convite, exigir re-consentimento quando a versão mudar, e exibir um aviso de cookies informativo.

**Architecture:** Camada legal central (`src/lib/legal.ts`: versões, DPO, helper de re-consentimento). Modelo `ConsentRecord` (auditável) gravado no ponto real de criação de conta (`verify-code/route.ts`) e no aceite de convite (`convite/[token]`). Páginas públicas estáticas + cookie notice client. Checagem de re-consentimento no layout do dashboard.

**Tech Stack:** Next.js 14 (App Router, server + client components), Prisma, Zod, Vitest, bcrypt (já em uso).

## Global Constraints

- **Não sou advogado:** todo texto legal é MINUTA e deve exibir um banner "Minuta — revisar com jurídico antes de produção". Não afirmar conformidade jurídica garantida.
- Versões de documento como constantes data-string (`'2026-06-22'`) em `src/lib/legal.ts`. Documentos: `POLITICA`, `TERMOS`.
- A criação de conta acontece em `src/app/api/auth/register/verify-code/route.ts` (NÃO em `register/route.ts`, que é órfão — não tocar nele neste sub-projeto).
- Cookies do app são só essenciais → aviso informativo, sem opt-in.
- Migração **aditiva**; se `prisma migrate dev` não rodar (TTY), escrever SQL à mão em `prisma/migrations/<timestamp>_lgpd_consent/migration.sql` + `npx prisma generate` + `npx prisma migrate deploy`.
- Suíte completa (398+) verde; tsc/lint limpos ao final.

---

### Task 1: Camada legal (`src/lib/legal.ts`) + testes

**Files:**
- Create: `src/lib/legal.ts`
- Test: `src/lib/__tests__/legal.test.ts`

**Interfaces:**
- Produces:
  - `export const POLITICA_VERSAO = '2026-06-22'`, `export const TERMOS_VERSAO = '2026-06-22'`
  - `export const DPO_CONTATO = { nome: 'Encarregado de Dados (DPO)', email: 'dpo@thefinance.app' }`
  - `export type ConsentDocumento = 'POLITICA' | 'TERMOS'`
  - `export const DOCUMENTOS_VIGENTES: { documento: ConsentDocumento; versao: string }[]`
  - `export function precisaReconsentir(aceitas: { documento: ConsentDocumento; versao: string }[], vigentes = DOCUMENTOS_VIGENTES): boolean`

- [ ] **Step 1: Teste que falha** (`src/lib/__tests__/legal.test.ts`):

```ts
import { describe, it, expect } from 'vitest'
import { precisaReconsentir } from '../legal'

const vigentes = [
  { documento: 'POLITICA' as const, versao: '2026-06-22' },
  { documento: 'TERMOS' as const, versao: '2026-06-22' },
]

describe('precisaReconsentir', () => {
  it('true quando falta um documento aceito', () => {
    expect(precisaReconsentir([{ documento: 'POLITICA', versao: '2026-06-22' }], vigentes)).toBe(true)
  })
  it('true quando a versão aceita é mais antiga', () => {
    expect(precisaReconsentir([
      { documento: 'POLITICA', versao: '2026-01-01' },
      { documento: 'TERMOS', versao: '2026-06-22' },
    ], vigentes)).toBe(true)
  })
  it('false quando todas as versões vigentes foram aceitas', () => {
    expect(precisaReconsentir([
      { documento: 'POLITICA', versao: '2026-06-22' },
      { documento: 'TERMOS', versao: '2026-06-22' },
    ], vigentes)).toBe(false)
  })
  it('considera a maior versão aceita por documento', () => {
    expect(precisaReconsentir([
      { documento: 'POLITICA', versao: '2026-01-01' },
      { documento: 'POLITICA', versao: '2026-06-22' },
      { documento: 'TERMOS', versao: '2026-06-22' },
    ], vigentes)).toBe(false)
  })
})
```

- [ ] **Step 2: Rodar e ver falhar** — `npx vitest run src/lib/__tests__/legal.test.ts` → FAIL (módulo não existe).

- [ ] **Step 3: Implementar** `src/lib/legal.ts`:

```ts
export type ConsentDocumento = 'POLITICA' | 'TERMOS'

export const POLITICA_VERSAO = '2026-06-22'
export const TERMOS_VERSAO = '2026-06-22'

export const DPO_CONTATO = { nome: 'Encarregado de Dados (DPO)', email: 'dpo@thefinance.app' }

export const DOCUMENTOS_VIGENTES: { documento: ConsentDocumento; versao: string }[] = [
  { documento: 'POLITICA', versao: POLITICA_VERSAO },
  { documento: 'TERMOS', versao: TERMOS_VERSAO },
]

/** true se faltar aceitar algum documento vigente ou se a maior versão aceita for menor que a vigente. */
export function precisaReconsentir(
  aceitas: { documento: ConsentDocumento; versao: string }[],
  vigentes = DOCUMENTOS_VIGENTES,
): boolean {
  return vigentes.some((v) => {
    const maiorAceita = aceitas
      .filter((a) => a.documento === v.documento)
      .map((a) => a.versao)
      .sort()
      .at(-1)
    return maiorAceita == null || maiorAceita < v.versao
  })
}
```
(Versões em formato `YYYY-MM-DD` comparam corretamente como string.)

- [ ] **Step 4: Rodar e ver passar** — `npx vitest run src/lib/__tests__/legal.test.ts` → PASS (4).

- [ ] **Step 5: Commit**
```bash
git add src/lib/legal.ts src/lib/__tests__/legal.test.ts
git commit -m "feat(lgpd): camada legal (versoes, DPO, precisaReconsentir)"
```

---

### Task 2: Modelo `ConsentRecord` + migração

**Files:**
- Modify: `prisma/schema.prisma` (novo model + enum + relação inversa em `User` perto de `accessLogs UserAccessLog[]`, ~linha 82)
- Create: `prisma/migrations/<timestamp>_lgpd_consent/migration.sql`

**Interfaces:**
- Produces: tabela `ConsentRecord` e enum `ConsentDoc` no Prisma Client.

- [ ] **Step 1: Editar `schema.prisma`.** Adicionar a relação inversa em `User` (junto de `accessLogs UserAccessLog[]`):
```prisma
  consentRecords      ConsentRecord[]
```
E no fim do arquivo (perto de outros models recentes):
```prisma
enum ConsentDoc {
  POLITICA
  TERMOS
}

model ConsentRecord {
  id        String     @id @default(cuid())
  userId    String
  tenantId  String?
  documento ConsentDoc
  versao    String
  aceitoEm  DateTime   @default(now())
  ip        String?
  user      User       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([userId, documento])
}
```

- [ ] **Step 2: Gerar a migração.** Tentar `npx prisma migrate dev --name lgpd_consent --create-only`. Se o ambiente não for interativo, criar a pasta `prisma/migrations/<YYYYMMDDHHMMSS>_lgpd_consent/` e escrever `migration.sql` à mão:
```sql
-- CreateEnum
CREATE TYPE "ConsentDoc" AS ENUM ('POLITICA', 'TERMOS');

-- CreateTable
CREATE TABLE "ConsentRecord" (
    "id" TEXT NOT NULL,
    "userId" TEXT NOT NULL,
    "tenantId" TEXT,
    "documento" "ConsentDoc" NOT NULL,
    "versao" TEXT NOT NULL,
    "aceitoEm" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "ip" TEXT,
    CONSTRAINT "ConsentRecord_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE INDEX "ConsentRecord_userId_idx" ON "ConsentRecord"("userId");
CREATE INDEX "ConsentRecord_userId_documento_idx" ON "ConsentRecord"("userId", "documento");

-- AddForeignKey
ALTER TABLE "ConsentRecord" ADD CONSTRAINT "ConsentRecord_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;
```

- [ ] **Step 3: Aplicar e gerar client** — `npx prisma generate` e (se DB disponível) `npx prisma migrate deploy`.

- [ ] **Step 4: Verificar tsc** — `npx tsc --noEmit` → limpo (o Prisma Client reconhece `prisma.consentRecord`).

- [ ] **Step 5: Commit**
```bash
git add prisma/schema.prisma prisma/migrations
git commit -m "feat(lgpd): modelo ConsentRecord + migracao"
```

---

### Task 3: Páginas Política/Termos + cookie notice + links no rodapé

**Files:**
- Create: `src/app/privacidade/page.tsx`, `src/app/termos/page.tsx`
- Create: `src/components/legal/MinutaBanner.tsx`, `src/components/legal/CookieNotice.tsx`
- Modify: `src/components/landing/Footer.tsx` (links), `src/app/layout.tsx` (montar `<CookieNotice/>`)

**Interfaces:**
- Consumes: `POLITICA_VERSAO`, `TERMOS_VERSAO`, `DPO_CONTATO` de `src/lib/legal.ts`.

- [ ] **Step 1: `MinutaBanner.tsx`** — componente server simples:
```tsx
export function MinutaBanner() {
  return (
    <div style={{ background: '#fde68a', color: '#713f12', padding: '10px 16px', borderRadius: 8, fontSize: 13, marginBottom: 24 }}>
      ⚠️ <strong>Minuta.</strong> Este documento é um modelo inicial e deve ser revisado por assessoria jurídica antes do uso em produção.
    </div>
  )
}
```

- [ ] **Step 2: `src/app/privacidade/page.tsx`** — server component. Renderiza `<MinutaBanner/>` + título "Política de Privacidade" + `Versão {POLITICA_VERSAO}` + seções (escrever o texto-modelo em pt-BR, prosa de minuta cobrindo cada tópico): **1. Controladora e Encarregado** (cita `DPO_CONTATO.email`); **2. Dados que coletamos** (cadastro: nome/e-mail/CNPJ; uso do sistema; dados de clientes do restaurante tratados como operador); **3. Finalidades e bases legais** (execução de contrato, obrigação legal/fiscal, legítimo interesse, consentimento); **4. Compartilhamento com operadores** (Focus NFe, iFood, Mercado Pago, WhatsApp/Evolution, Cloudinary); **5. Retenção**; **6. Direitos do titular** (acesso, correção, exclusão, portabilidade, revogação — menciona que serão atendidos por canais do sistema/no DPO); **7. Cookies** (apenas essenciais: sessão/auth/impersonation/unidade ativa); **8. Segurança**; **9. Alterações desta política**; **10. Contato**. Layout simples centralizado (maxWidth ~760, padding), legível.

- [ ] **Step 3: `src/app/termos/page.tsx`** — server component. `<MinutaBanner/>` + "Termos de Uso" + `Versão {TERMOS_VERSAO}` + seções de minuta: objeto, cadastro/conta, responsabilidades do usuário, planos/pagamento, propriedade intelectual, limitação de responsabilidade, rescisão, lei aplicável/foro, contato. Mesmo layout.

- [ ] **Step 4: `CookieNotice.tsx`** — client component (`'use client'`), banner inferior dispensável; lê/grava `localStorage['tf-cookie-notice'] === 'ok'`; texto "Usamos apenas cookies essenciais para o funcionamento do sistema." + link `/privacidade` + botão "Entendi" que grava e fecha. Não renderiza nada se já dispensado.

- [ ] **Step 5: Montar e linkar.** Em `src/app/layout.tsx`, renderizar `<CookieNotice/>` dentro do `<body>` (após `children`). Em `src/components/landing/Footer.tsx`, adicionar links `Política de Privacidade` (`/privacidade`) e `Termos` (`/termos`).

- [ ] **Step 6: Verificar** — `npx tsc --noEmit` limpo; `npx next lint --file src/app/privacidade/page.tsx --file src/app/termos/page.tsx --file src/components/legal/CookieNotice.tsx` sem novos erros. Abrir mentalmente: páginas acessíveis sem login.

- [ ] **Step 7: Commit**
```bash
git add src/app/privacidade src/app/termos src/components/legal src/app/layout.tsx src/components/landing/Footer.tsx
git commit -m "feat(lgpd): paginas privacidade/termos (minuta) + cookie notice + links"
```

---

### Task 4: Consentimento no cadastro

**Files:**
- Modify: `src/lib/validations.ts` (`registerSchema` + `aceiteLgpd`)
- Modify: `src/app/auth/register/page.tsx` (checkbox + enviar no payload)
- Modify: `src/app/api/auth/register/verify-code/route.ts` (gravar `ConsentRecord` ao criar a conta)
- Test: `src/app/api/auth/register/__tests__/verify-code.consent.test.ts`

**Interfaces:**
- Consumes: `POLITICA_VERSAO`, `TERMOS_VERSAO` de `src/lib/legal.ts`; `getClientIp` de `src/lib/rate-limit.ts`.

- [ ] **Step 1: Schema.** Em `src/lib/validations.ts`, adicionar ao `registerSchema`:
```ts
  aceiteLgpd: z.literal(true, { errorMap: () => ({ message: 'É necessário aceitar a Política de Privacidade e os Termos' }) }),
```

- [ ] **Step 2: Teste que falha** (`verify-code.consent.test.ts`). Mockar `@/lib/prisma` com `emailVerificationCode.findFirst` (retorna registro válido não usado), `user.findUnique`/`tenant.findUnique` (null), `tenant.create` (retorna `{ id: 't1', users: [{ id: 'u1' }] }`), `emailVerificationCode.update`, e `consentRecord.createMany`. Postar payload válido com `aceiteLgpd: true`. Asserir que `consentRecord.createMany` foi chamado com 2 itens (POLITICA+TERMOS) contendo `userId: 'u1'`, `versao` correta e `documento` correto. (Espelhar o estilo de mock dos testes existentes em `src/app/api/**/__tests__` ou `src/services/**/__tests__`.)

- [ ] **Step 3: Implementar no `verify-code/route.ts`.** No `tenant.create`, garantir o id do user criado (`include: { users: true }`); após criar, gravar consentimento (mesmo IP do request):
```ts
import { getClientIp } from '@/lib/rate-limit'
import { POLITICA_VERSAO, TERMOS_VERSAO } from '@/lib/legal'
// ...
const ip = getClientIp(req)
const tenant = await prisma.tenant.create({
  data: { /* ...existente... */ },
  include: { users: true },
})
const adminUser = tenant.users[0]
await prisma.consentRecord.createMany({
  data: [
    { userId: adminUser.id, tenantId: tenant.id, documento: 'POLITICA', versao: POLITICA_VERSAO, ip },
    { userId: adminUser.id, tenantId: tenant.id, documento: 'TERMOS', versao: TERMOS_VERSAO, ip },
  ],
})
```
(Se `req` não estiver acessível para `getClientIp`, capturar o IP no início do handler como já é feito em send-code.)

- [ ] **Step 4: Form.** Em `src/app/auth/register/page.tsx`: adicionar `aceiteLgpd: false` ao `formData`; um checkbox obrigatório com label "Li e aceito a <a href='/privacidade'>Política de Privacidade</a> e os <a href='/termos'>Termos</a>"; bloquear o submit do passo 1 se desmarcado; incluir `aceiteLgpd: formData.aceiteLgpd` nos bodies dos fetch de `send-code` e `verify-code`.

- [ ] **Step 5: Rodar testes** — `npx vitest run src/app/api/auth/register/__tests__/verify-code.consent.test.ts` → PASS. `npx tsc --noEmit` limpo.

- [ ] **Step 6: Commit**
```bash
git add src/lib/validations.ts src/app/auth/register/page.tsx src/app/api/auth/register/verify-code/route.ts src/app/api/auth/register/__tests__/verify-code.consent.test.ts
git commit -m "feat(lgpd): consentimento obrigatorio no cadastro (registro auditavel)"
```

---

### Task 5: Consentimento no aceite de convite

**Files:**
- Modify: `src/app/api/convite/[token]/route.ts` (gravar `ConsentRecord` na `$transaction` que cria o user)
- Modify: o formulário de aceite de convite (localizar a página que faz POST para `convite/[token]`; adicionar checkbox + enviar `aceiteLgpd`)
- Modify: o `zod` schema inline do `convite/[token]` (adicionar `aceiteLgpd: z.literal(true)`)
- Test: `src/app/api/convite/__tests__/aceite.consent.test.ts`

**Interfaces:**
- Consumes: `POLITICA_VERSAO`, `TERMOS_VERSAO`, `getClientIp`.

- [ ] **Step 1: Localizar o form de aceite** — `grep -rn "convite/" src/app --include=*.tsx`. Adicionar `aceiteLgpd` ao schema inline do route, o checkbox no form, e o envio no payload (mesmo padrão da Task 4).

- [ ] **Step 2: Teste que falha** (`aceite.consent.test.ts`). Mock `@/lib/prisma`: `invite.findUnique` (válido/pendente), `user.findFirst` (null), `$transaction` resolvendo. Postar com `aceiteLgpd: true`. Asserir que o array passado ao `$transaction` inclui uma chamada `consentRecord.createMany` (ou dois `consentRecord.create`) com `userId` do user criado, 2 documentos e versões corretas. Como o `userId` é gerado dentro da transação, ajustar: gerar o `id` do user com `cuid()`/`createId` antes (ou usar `user.create` retornando id fora da transação seguido de `createMany`). Escolher a abordagem que o handler já permite; documentar no teste o formato esperado.

- [ ] **Step 3: Implementar** — após criar o user (capturando o id), incluir a gravação de 2 `ConsentRecord` (POLITICA+TERMOS, versão atual, `ip`, `tenantId` do invite). Manter atomicidade: se usar `$transaction([...])`, gerar o user id antecipadamente (`import { createId } from '@paralleldrive/cuid2'` se já disponível; senão criar o user e depois `createMany` — aceitável).

- [ ] **Step 4: Rodar testes** — `npx vitest run src/app/api/convite/__tests__/aceite.consent.test.ts` → PASS. `npx tsc --noEmit` limpo.

- [ ] **Step 5: Commit**
```bash
git add src/app/api/convite src/app/**/convite* src/app/api/convite/__tests__
git commit -m "feat(lgpd): consentimento no aceite de convite"
```

---

### Task 6: Re-consentimento por versão

**Files:**
- Create: `src/app/consentimento/page.tsx` (re-aceite) + uma rota `src/app/api/consentimento/route.ts` (POST grava novos `ConsentRecord`)
- Modify: `src/app/(dashboard)/layout.tsx` (checar `precisaReconsentir` e redirecionar)

**Interfaces:**
- Consumes: `precisaReconsentir`, `DOCUMENTOS_VIGENTES`, versões; sessão (`getSession`).

- [ ] **Step 1: Rota POST `src/app/api/consentimento/route.ts`.** Auth via `getSession`; cria `consentRecord.createMany` para os `DOCUMENTOS_VIGENTES` ainda não aceitos pelo usuário (userId da sessão, tenantId, ip). Retorna `{ ok: true }`.

- [ ] **Step 2: Checagem no layout do dashboard.** Em `src/app/(dashboard)/layout.tsx` (server): obter sessão; se logado, buscar `prisma.consentRecord.findMany({ where: { userId }, select: { documento: true, versao: true } })`; se `precisaReconsentir(aceitas)` → `redirect('/consentimento')`. (Usar `next/navigation` `redirect`.) Não aplicar a checagem na própria rota `/consentimento`.

- [ ] **Step 3: Página `src/app/consentimento/page.tsx`** (client): explica que os documentos foram atualizados, links para `/privacidade` e `/termos`, checkbox de aceite, botão que faz POST em `/api/consentimento` e em sucesso `router.push('/dashboard')` (ou rota inicial do dashboard).

- [ ] **Step 4: Teste.** Cobertura unitária do `precisaReconsentir` já existe (Task 1). Para a rota, teste leve mockando `@/lib/prisma` + sessão: POST cria os `ConsentRecord` faltantes. (`src/app/api/consentimento/__tests__/route.test.ts`.)

- [ ] **Step 5: Verificar** — `npx vitest run src/app/api/consentimento/__tests__/route.test.ts` PASS; `npx tsc --noEmit` limpo.

- [ ] **Step 6: Commit**
```bash
git add src/app/consentimento src/app/api/consentimento "src/app/(dashboard)/layout.tsx"
git commit -m "feat(lgpd): re-consentimento por versao no acesso ao dashboard"
```

---

### Task 7: Verificação final

- [ ] **Step 1:** `npx tsc --noEmit` → limpo.
- [ ] **Step 2:** `npx vitest run` → todos verdes (398 + novos).
- [ ] **Step 3:** `npx next lint` (ou nos arquivos tocados) → sem novos erros.
- [ ] **Step 4:** Depois: finishing-a-development-branch.

---

## Self-Review

- **Cobertura da spec:** páginas legais → Task 3; `ConsentRecord` → Task 2; consentimento no cadastro → Task 4; convite → Task 5; re-consentimento → Tasks 1+6; cookie notice → Task 3; DPO → Tasks 1+3. ✔
- **Correção vs spec:** spec citava `register/route.ts`; o ponto real de criação é `verify-code/route.ts` (register é órfão, não tocar) — refletido na Task 4. ✔
- **Placeholders:** Tasks de lógica têm código/SQL/testes concretos; Task 3 é conteúdo (prosa de minuta) com outline obrigatório por seção; Task 5 deixa explícita a escolha de captura do user id (sem "TODO"). ✔
- **Consistência de tipos:** `ConsentDocumento`/`ConsentDoc`, `precisaReconsentir`, versões e `createMany` usados de forma consistente entre tasks. ✔
