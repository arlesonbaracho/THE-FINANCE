# LGPD G2b — Direitos do titular da própria conta — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) ou superpowers:executing-plans. Steps usam checkbox (`- [ ]`).

**Goal:** Permitir que o titular (usuário do restaurante) exporte seus próprios dados (JSON) e exclua a própria conta via anonimização, com guarda para o último ADMIN.

**Architecture:** Serviço puro/tenant-scoped faz export e anonimização; rotas finas expõem para a sessão do próprio usuário; UI em Configurações. Exclusão = anonimização (preserva referências operacionais/fiscais). `prisma migrate dev` não roda em TTY → migração à mão + `prisma generate`.

**Tech Stack:** Next.js 14 route handlers, Prisma/PostgreSQL, NextAuth (`getSession`/`signOut`), Vitest.

## Global Constraints

- Exclusão = **anonimização**, nunca hard-delete. Preserva `id` e FKs operacionais (Pedido/SessaoCaixa/PurchaseOrder/Brand).
- O **único ADMIN ativo** do tenant não pode se anonimizar → 409.
- Export JSON nunca inclui `password` nem `pin`.
- Migração additiva, escrita à mão (formato das migrações existentes), timestamp > `20260623140000`; rodar `npx prisma generate` para o client.
- Suíte completa verde; tsc/lint limpos.

---

### Task 1: Schema `User.anonimizadoEm` + migração

**Files:**
- Modify: `prisma/schema.prisma` (model `User`)
- Create: `prisma/migrations/20260624100000_user_anonimizado/migration.sql`

- [ ] **Step 1:** No model `User`, após `passwordChangedAt DateTime?`, adicionar:

```prisma
  anonimizadoEm       DateTime?
```

- [ ] **Step 2:** Criar `prisma/migrations/20260624100000_user_anonimizado/migration.sql`:

```sql
-- AlterTable
ALTER TABLE "User" ADD COLUMN "anonimizadoEm" TIMESTAMP(3);
```

- [ ] **Step 3:** Regenerar o client (não precisa de DB):

Run: `npx prisma generate`
Expected: sucesso; o client passa a conhecer `anonimizadoEm`. NÃO rodar `migrate dev`. `migrate deploy` pode ser pulado se não houver DB.

- [ ] **Step 4:** Verificar tsc.

Run: `npx tsc --noEmit`
Expected: limpo.

- [ ] **Step 5: Commit**

```bash
git add prisma/schema.prisma prisma/migrations/20260624100000_user_anonimizado
git commit -m "feat(lgpd): campo User.anonimizadoEm + migracao"
```

---

### Task 2: Serviço `dados-pessoais.service.ts`

**Files:**
- Create: `src/services/lgpd/dados-pessoais.service.ts`
- Test: `src/services/lgpd/__tests__/dados-pessoais.service.test.ts`

**Interfaces:**
- Produces: `exportarDadosUsuario(userId: string): Promise<DadosExportados>`; `anonimizarUsuario(userId: string): Promise<void>`; `contarAdminsAtivos(tenantId: string): Promise<number>`; `podeAnonimizar(usuario: { role: string; tenantId: string | null }, totalAdminsAtivos: number): boolean` (pura).

- [ ] **Step 1: Testes que falham.** Criar o teste com `vi.mock('@/lib/prisma', () => ({ prisma: { user: { findUnique: vi.fn(), update: vi.fn(), count: vi.fn() }, consentRecord: { findMany: vi.fn() }, userAccessLog: { findMany: vi.fn() } } }))`:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
vi.mock('@/lib/prisma', () => ({ prisma: { user: { findUnique: vi.fn(), update: vi.fn(), count: vi.fn() }, consentRecord: { findMany: vi.fn() }, userAccessLog: { findMany: vi.fn() } } }))
import { exportarDadosUsuario, anonimizarUsuario, podeAnonimizar } from '../dados-pessoais.service'
import { prisma } from '@/lib/prisma'
const mp = prisma as any
beforeEach(() => vi.clearAllMocks())

describe('podeAnonimizar', () => {
  it('bloqueia o unico ADMIN ativo', () => {
    expect(podeAnonimizar({ role: 'ADMIN', tenantId: 't1' }, 1)).toBe(false)
  })
  it('permite ADMIN quando ha outros admins', () => {
    expect(podeAnonimizar({ role: 'ADMIN', tenantId: 't1' }, 2)).toBe(true)
  })
  it('permite staff/manager', () => {
    expect(podeAnonimizar({ role: 'STAFF', tenantId: 't1' }, 1)).toBe(true)
    expect(podeAnonimizar({ role: 'MANAGER', tenantId: 't1' }, 1)).toBe(true)
  })
})

describe('exportarDadosUsuario', () => {
  it('retorna perfil+consentimentos+logs sem password/pin', async () => {
    mp.user.findUnique.mockResolvedValue({ id: 'u1', name: 'Ana', email: 'a@x.com', role: 'STAFF', status: 'ACTIVE', createdAt: new Date(), updatedAt: new Date(), ultimoAcesso: null, avatarUrl: null })
    mp.consentRecord.findMany.mockResolvedValue([{ documento: 'POLITICA', versao: '2026-06-22', aceitoEm: new Date() }])
    mp.userAccessLog.findMany.mockResolvedValue([{ createdAt: new Date() }])
    const r = await exportarDadosUsuario('u1') as any
    expect(r.perfil.id).toBe('u1')
    expect(JSON.stringify(r)).not.toMatch(/password|pin/i)
    expect(r.consentimentos).toHaveLength(1)
    expect(r.logsAcesso).toHaveLength(1)
    // o select do findUnique nao pede password/pin
    const sel = mp.user.findUnique.mock.calls[0][0].select
    expect(sel.password).toBeUndefined()
    expect(sel.pin).toBeUndefined()
  })
})

describe('anonimizarUsuario', () => {
  it('limpa dados pessoais, inativa e seta anonimizadoEm', async () => {
    mp.user.update.mockResolvedValue({})
    await anonimizarUsuario('u1')
    const data = mp.user.update.mock.calls[0][0].data
    expect(mp.user.update.mock.calls[0][0].where).toEqual({ id: 'u1' })
    expect(data.name).toBeNull()
    expect(data.email).toBe('anonimizado+u1@removido.local')
    expect(data.pin).toBeNull()
    expect(data.password).toBeNull()
    expect(data.status).toBe('INACTIVE')
    expect(data.anonimizadoEm).toBeInstanceOf(Date)
  })
})
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run src/services/lgpd/__tests__/dados-pessoais.service.test.ts`
Expected: FAIL (módulo não existe).

- [ ] **Step 3: Implementar o serviço:**

```ts
import { prisma } from '@/lib/prisma'

export type DadosExportados = {
  perfil: Record<string, unknown>
  consentimentos: unknown[]
  logsAcesso: unknown[]
}

export async function exportarDadosUsuario(userId: string): Promise<DadosExportados> {
  const perfil = await prisma.user.findUnique({
    where: { id: userId },
    select: { id: true, name: true, email: true, role: true, status: true, avatarUrl: true, ultimoAcesso: true, createdAt: true, updatedAt: true, anonimizadoEm: true },
  })
  const consentimentos = await prisma.consentRecord.findMany({
    where: { userId },
    select: { documento: true, versao: true, aceitoEm: true, ip: true },
    orderBy: { aceitoEm: 'asc' },
  })
  const logsAcesso = await prisma.userAccessLog.findMany({
    where: { userId },
    orderBy: { createdAt: 'desc' },
    take: 200,
  })
  return { perfil: perfil ?? {}, consentimentos, logsAcesso }
}

export async function anonimizarUsuario(userId: string): Promise<void> {
  await prisma.user.update({
    where: { id: userId },
    data: {
      name: null,
      email: `anonimizado+${userId}@removido.local`,
      avatarUrl: null,
      image: null,
      pin: null,
      password: null,
      status: 'INACTIVE',
      anonimizadoEm: new Date(),
    },
  })
}

export async function contarAdminsAtivos(tenantId: string): Promise<number> {
  return prisma.user.count({
    where: { tenantId, role: 'ADMIN', status: 'ACTIVE', anonimizadoEm: null },
  })
}

export function podeAnonimizar(usuario: { role: string; tenantId: string | null }, totalAdminsAtivos: number): boolean {
  if (usuario.role === 'ADMIN') return totalAdminsAtivos > 1
  return true
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run src/services/lgpd/__tests__/dados-pessoais.service.test.ts`
Expected: PASS (todos).

- [ ] **Step 5: Commit**

```bash
git add src/services/lgpd/dados-pessoais.service.ts src/services/lgpd/__tests__/dados-pessoais.service.test.ts
git commit -m "feat(lgpd): servico de exportacao/anonimizacao de dados do titular"
```

---

### Task 3: Rotas `exportar` e `excluir`

**Files:**
- Create: `src/app/api/conta/exportar/route.ts`
- Create: `src/app/api/conta/excluir/route.ts`
- Test: `src/app/api/conta/__tests__/excluir.route.test.ts`

**Interfaces:**
- Consumes: `exportarDadosUsuario`, `anonimizarUsuario`, `contarAdminsAtivos`, `podeAnonimizar` (Task 2); `getSession` de `@/lib/session`.

- [ ] **Step 1: `exportar/route.ts`:**

```ts
import { NextResponse } from 'next/server'
import { getSession, unauthorizedResponse } from '@/lib/session'
import { exportarDadosUsuario } from '@/services/lgpd/dados-pessoais.service'

export async function GET() {
  const session = await getSession()
  if (!session?.user?.id) return unauthorizedResponse()
  const dados = await exportarDadosUsuario(session.user.id)
  return new NextResponse(JSON.stringify(dados, null, 2), {
    status: 200,
    headers: {
      'Content-Type': 'application/json',
      'Content-Disposition': 'attachment; filename="meus-dados.json"',
    },
  })
}
```

- [ ] **Step 2: Teste da rota `excluir` que falha:**

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
vi.mock('@/lib/session', () => ({ getSession: vi.fn(), unauthorizedResponse: () => new Response(JSON.stringify({ error: 'Não autorizado' }), { status: 401 }) }))
vi.mock('@/services/lgpd/dados-pessoais.service', () => ({
  contarAdminsAtivos: vi.fn(), anonimizarUsuario: vi.fn(),
  podeAnonimizar: (u: any, n: number) => (u.role === 'ADMIN' ? n > 1 : true),
}))
import { POST } from '../../conta/excluir/route'
import { getSession } from '@/lib/session'
import { contarAdminsAtivos, anonimizarUsuario } from '@/services/lgpd/dados-pessoais.service'

beforeEach(() => vi.clearAllMocks())

it('bloqueia o unico admin (409) sem anonimizar', async () => {
  ;(getSession as any).mockResolvedValue({ user: { id: 'u1', tenantId: 't1', role: 'ADMIN' } })
  ;(contarAdminsAtivos as any).mockResolvedValue(1)
  const res = await POST()
  expect(res.status).toBe(409)
  expect(anonimizarUsuario).not.toHaveBeenCalled()
})

it('anonimiza staff (ok)', async () => {
  ;(getSession as any).mockResolvedValue({ user: { id: 'u2', tenantId: 't1', role: 'STAFF' } })
  ;(contarAdminsAtivos as any).mockResolvedValue(1)
  ;(anonimizarUsuario as any).mockResolvedValue(undefined)
  const res = await POST()
  expect(res.status).toBe(200)
  expect(anonimizarUsuario).toHaveBeenCalledWith('u2')
})
```

Nota: ajustar o caminho relativo do import de `POST` conforme a localização do teste (`src/app/api/conta/__tests__/` → `../excluir/route`).

- [ ] **Step 3: Rodar e ver falhar**

Run: `npx vitest run src/app/api/conta/__tests__/excluir.route.test.ts`
Expected: FAIL (rota não existe).

- [ ] **Step 4: `excluir/route.ts`:**

```ts
import { NextResponse } from 'next/server'
import { getSession, unauthorizedResponse } from '@/lib/session'
import { contarAdminsAtivos, anonimizarUsuario, podeAnonimizar } from '@/services/lgpd/dados-pessoais.service'

export async function POST() {
  const session = await getSession()
  if (!session?.user?.id) return unauthorizedResponse()
  const { id, tenantId, role } = session.user
  const totalAdmins = tenantId ? await contarAdminsAtivos(tenantId) : 0
  if (!podeAnonimizar({ role, tenantId }, totalAdmins)) {
    return NextResponse.json(
      { error: 'Você é o único administrador da conta. Transfira a administração ou encerre a conta antes de excluir seu acesso.' },
      { status: 409 },
    )
  }
  await anonimizarUsuario(id)
  return NextResponse.json({ ok: true })
}
```

Corrigir o import relativo no teste (Step 2) se necessário para `../excluir/route`.

- [ ] **Step 5: Rodar e ver passar**

Run: `npx vitest run src/app/api/conta/__tests__/excluir.route.test.ts`
Expected: PASS. Depois `npx tsc --noEmit` limpo. (Se `session.user.role` não existir no tipo da sessão, conferir o tipo de `next-auth` do projeto — `role` já é usado em outras rotas, ex.: `fiscal/sincronizar/route.ts`.)

- [ ] **Step 6: Commit**

```bash
git add src/app/api/conta/exportar/route.ts src/app/api/conta/excluir/route.ts src/app/api/conta/__tests__/excluir.route.test.ts
git commit -m "feat(lgpd): rotas de exportacao e exclusao (anonimizacao) da propria conta"
```

---

### Task 4: UI "Privacidade e meus dados"

**Files:**
- Create: `src/app/(dashboard)/configuracoes/privacidade/page.tsx`
- Modify: a navegação de Configurações (mesmo lugar que lista `perfil`/`usuarios` — procurar com `grep -rn "configuracoes/perfil" src/`)

- [ ] **Step 1: Página (client component)** com tema `--tf-*`:
  - Cabeçalho "Privacidade e meus dados".
  - Card "Seus dados": botão **"Baixar meus dados"** → `fetch('/api/conta/exportar')` → `blob()` → âncora download `meus-dados.json`.
  - Card "Excluir minha conta": aviso de irreversibilidade; input de texto que exige digitar `EXCLUIR` (botão desabilitado até bater); ao clicar → `fetch('/api/conta/excluir', { method: 'POST' })`; se `res.ok` → `signOut({ callbackUrl: '/auth/login' })` (de `next-auth/react`); se `409` → mostrar a mensagem do corpo. Estados loading/erro.

- [ ] **Step 2: Link na navegação.** Adicionar item "Privacidade" → `/configuracoes/privacidade` onde os outros itens de configurações são listados (seguir o padrão/estilo do item `perfil`).

- [ ] **Step 3: Verificar**

Run: `npx tsc --noEmit` (limpo) e `npx next lint --file "src/app/(dashboard)/configuracoes/privacidade/page.tsx"` (sem novos erros).

- [ ] **Step 4: Commit**

```bash
git add "src/app/(dashboard)/configuracoes/privacidade/page.tsx" <arquivo de navegacao>
git commit -m "feat(lgpd): UI Privacidade e meus dados (exportar/excluir conta)"
```

---

### Task 5: Verificação final

- [ ] **Step 1:** `npx tsc --noEmit` → limpo.
- [ ] **Step 2:** `npx vitest run` → todos verdes (410 + novos).
- [ ] **Step 3:** `npx next lint` (arquivos tocados) → sem novos erros.
- [ ] **Step 4:** Depois: finishing-a-development-branch.

---

## Self-Review

- **Cobertura da spec:** schema `anonimizadoEm` → T1; export/anonimizar/podeAnonimizar → T2; rotas + guarda 409 → T3; UI → T4; verificação → T5. ✔
- **Placeholders:** nenhum "TODO"; código completo em cada step. UI (T4) descreve comportamento concreto + a navegação é localizada por grep (padrão do projeto). ✔
- **Consistência de tipos:** `podeAnonimizar(usuario, total)` assinatura idêntica em T2/T3; `anonimizarUsuario(userId)`, `contarAdminsAtivos(tenantId)`, `exportarDadosUsuario(userId)` consistentes. O e-mail anonimizado (`anonimizado+<id>@removido.local`) idêntico em serviço e teste. ✔
