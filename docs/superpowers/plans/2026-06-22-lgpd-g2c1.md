# LGPD G2c-1 — Retenção/expurgo automático — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) ou superpowers:executing-plans. Steps usam checkbox (`- [ ]`).

**Goal:** Job BullMQ diário que expurga dados transitórios expirados (códigos de verificação, tokens de reset) e logs além da janela de retenção (12 meses), dando efeito ao princípio de minimização/retenção da LGPD.

**Architecture:** Serviço puro `expurgarDadosAntigos` faz os `deleteMany`; um job BullMQ o invoca; a fila + o agendamento seguem exatamente o padrão de `nf-capture` (cron pattern via env). Platform-wide.

**Tech Stack:** BullMQ (filas em `src/lib/queues.ts`, workers em `src/jobs/**`, registro em `src/jobs/worker.ts`), Prisma, Vitest.

## Global Constraints

- Expurgo **platform-wide** (não por tenant). Sem botão manual. Sem `Reserva`.
- Transitórios: apagar onde `expiresAt < now`. Logs: apagar onde `createdAt < corte`, `corte = now − RETENCAO_LOGS_MESES meses`.
- `RETENCAO_LOGS_MESES` default 12; `EXPURGO_INTERVAL_HOURS` default 24.
- Seguir o padrão existente (fila + `criar...Worker` + `upsertJobScheduler` com cron pattern). Suíte verde; tsc/lint limpos.

---

### Task 1: Serviço `expurgo.service.ts`

**Files:**
- Create: `src/services/lgpd/expurgo.service.ts`
- Test: `src/services/lgpd/__tests__/expurgo.service.test.ts`

**Interfaces:**
- Produces: `expurgarDadosAntigos({ retencaoLogsMeses }: { retencaoLogsMeses: number }): Promise<ResultadoExpurgo>` onde `ResultadoExpurgo = { codigosVerificacao: number; tokensReset: number; logsAcesso: number; logsWhatsapp: number }`.

- [ ] **Step 1: Teste que falha.** Criar `src/services/lgpd/__tests__/expurgo.service.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
vi.mock('@/lib/prisma', () => ({ prisma: {
  emailVerificationCode: { deleteMany: vi.fn() },
  passwordResetToken: { deleteMany: vi.fn() },
  userAccessLog: { deleteMany: vi.fn() },
  whatsAppLog: { deleteMany: vi.fn() },
} }))
import { expurgarDadosAntigos } from '../expurgo.service'
import { prisma } from '@/lib/prisma'
const mp = prisma as any
beforeEach(() => {
  vi.clearAllMocks()
  mp.emailVerificationCode.deleteMany.mockResolvedValue({ count: 3 })
  mp.passwordResetToken.deleteMany.mockResolvedValue({ count: 2 })
  mp.userAccessLog.deleteMany.mockResolvedValue({ count: 10 })
  mp.whatsAppLog.deleteMany.mockResolvedValue({ count: 5 })
})

describe('expurgarDadosAntigos', () => {
  it('apaga transitorios expirados e logs alem do corte, retorna contagens', async () => {
    const r = await expurgarDadosAntigos({ retencaoLogsMeses: 12 })
    expect(r).toEqual({ codigosVerificacao: 3, tokensReset: 2, logsAcesso: 10, logsWhatsapp: 5 })

    // transitorios: expiresAt < now
    const codeWhere = mp.emailVerificationCode.deleteMany.mock.calls[0][0].where
    expect(codeWhere.expiresAt.lt).toBeInstanceOf(Date)
    const tokenWhere = mp.passwordResetToken.deleteMany.mock.calls[0][0].where
    expect(tokenWhere.expiresAt.lt).toBeInstanceOf(Date)

    // logs: createdAt < corte (12 meses atras)
    const logWhere = mp.userAccessLog.deleteMany.mock.calls[0][0].where
    const corte: Date = logWhere.createdAt.lt
    expect(corte).toBeInstanceOf(Date)
    const esperado = new Date(); esperado.setMonth(esperado.getMonth() - 12)
    // mesmo ano/mes do corte
    expect(corte.getFullYear()).toBe(esperado.getFullYear())
    expect(corte.getMonth()).toBe(esperado.getMonth())
    const waWhere = mp.whatsAppLog.deleteMany.mock.calls[0][0].where
    expect(waWhere.createdAt.lt).toBeInstanceOf(Date)
  })

  it('respeita a janela configurada (6 meses)', async () => {
    await expurgarDadosAntigos({ retencaoLogsMeses: 6 })
    const corte: Date = mp.userAccessLog.deleteMany.mock.calls[0][0].where.createdAt.lt
    const esperado = new Date(); esperado.setMonth(esperado.getMonth() - 6)
    expect(corte.getMonth()).toBe(esperado.getMonth())
  })
})
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run src/services/lgpd/__tests__/expurgo.service.test.ts`
Expected: FAIL (módulo não existe).

- [ ] **Step 3: Implementar:**

```ts
import { prisma } from '@/lib/prisma'

export type ResultadoExpurgo = {
  codigosVerificacao: number
  tokensReset: number
  logsAcesso: number
  logsWhatsapp: number
}

/**
 * Expurga dados transitórios expirados e logs além da janela de retenção.
 * Platform-wide (manutenção). `retencaoLogsMeses` define o corte dos logs.
 */
export async function expurgarDadosAntigos(
  { retencaoLogsMeses }: { retencaoLogsMeses: number },
): Promise<ResultadoExpurgo> {
  const now = new Date()
  const corte = new Date(now)
  corte.setMonth(corte.getMonth() - retencaoLogsMeses)

  const [codigos, tokens, logsAcesso, logsWa] = await Promise.all([
    prisma.emailVerificationCode.deleteMany({ where: { expiresAt: { lt: now } } }),
    prisma.passwordResetToken.deleteMany({ where: { expiresAt: { lt: now } } }),
    prisma.userAccessLog.deleteMany({ where: { createdAt: { lt: corte } } }),
    prisma.whatsAppLog.deleteMany({ where: { createdAt: { lt: corte } } }),
  ])

  return {
    codigosVerificacao: codigos.count,
    tokensReset: tokens.count,
    logsAcesso: logsAcesso.count,
    logsWhatsapp: logsWa.count,
  }
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run src/services/lgpd/__tests__/expurgo.service.test.ts`
Expected: PASS (ambos). Depois `npx tsc --noEmit` limpo.

- [ ] **Step 5: Commit**

```bash
git add src/services/lgpd/expurgo.service.ts src/services/lgpd/__tests__/expurgo.service.test.ts
git commit -m "feat(lgpd): servico de expurgo de dados antigos (retencao)"
```

---

### Task 2: Fila + job + registro no worker

**Files:**
- Modify: `src/lib/queues.ts` (adicionar `expurgoQueue`)
- Create: `src/jobs/lgpd/expurgo.job.ts`, `src/jobs/lgpd/index.ts`
- Modify: `src/jobs/worker.ts`

**Interfaces:**
- Consumes: `expurgarDadosAntigos` (Task 1).
- Produces: `expurgoQueue` (fila BullMQ nome `'lgpd-expurgo'`); `processExpurgoJob()`; `criarExpurgoWorker()`.

- [ ] **Step 1: `expurgoQueue` em `src/lib/queues.ts`.** Após `nfceRetryQueue`:

```ts
export const expurgoQueue = new Queue('lgpd-expurgo', {
  connection: redisConnectionOptions,
  defaultJobOptions: { attempts: 2, backoff: { type: 'fixed', delay: 5000 } },
})
```
E adicionar `expurgoQueue` ao array do loop de `.on('error', ...)` existente:
`for (const q of [platformHealthQueue, saasMetricsQueue, nfCaptureQueue, nfceRetryQueue, expurgoQueue]) {`

- [ ] **Step 2: `src/jobs/lgpd/expurgo.job.ts`:**

```ts
import { expurgarDadosAntigos } from '@/services/lgpd/expurgo.service'

export async function processExpurgoJob(): Promise<void> {
  const retencaoLogsMeses = parseInt(process.env.RETENCAO_LOGS_MESES ?? '12', 10)
  const r = await expurgarDadosAntigos({ retencaoLogsMeses })
  console.log(
    `[lgpd-expurgo] códigos=${r.codigosVerificacao} tokens=${r.tokensReset} ` +
    `logsAcesso=${r.logsAcesso} logsWhatsapp=${r.logsWhatsapp}`,
  )
}
```

- [ ] **Step 3: `src/jobs/lgpd/index.ts`** (espelhar `src/jobs/fiscal/index.ts`):

```ts
import { Worker, type Job } from 'bullmq'
import { redisConnectionOptions } from '@/lib/bullmq'
import { processExpurgoJob } from './expurgo.job'

export function criarExpurgoWorker() {
  const worker = new Worker(
    'lgpd-expurgo',
    async (_job: Job) => {
      await processExpurgoJob()
    },
    { connection: redisConnectionOptions },
  )
  worker.on('failed', (job, err) => {
    console.error(`[lgpd-expurgo] Job ${job?.id} falhou:`, err.message)
  })
  return worker
}
```

- [ ] **Step 4: Registrar em `src/jobs/worker.ts`.**
  - Import: na linha das filas, adicionar `expurgoQueue`; nova linha `import { criarExpurgoWorker } from './lgpd/index'`.
  - Constante: `const EXPURGO_INTERVAL_HOURS = parseInt(process.env.EXPURGO_INTERVAL_HOURS ?? '24', 10)`.
  - Scheduler (após o de `nfceRetryQueue`):
    ```ts
    await expurgoQueue.upsertJobScheduler(
      'lgpd-expurgo-cron',
      { pattern: `0 */${EXPURGO_INTERVAL_HOURS} * * *` },
      { name: 'lgpd-expurgo', data: {} }
    )
    ```
  - Worker: `criarExpurgoWorker()` (junto dos outros `criar...`).
  - Log: `console.log(\`[worker] lgpd-expurgo: a cada ${EXPURGO_INTERVAL_HOURS}h\`)`.

- [ ] **Step 5: Verificar**

Run: `npx tsc --noEmit`
Expected: limpo (imports resolvem, fila/worker tipados).
Run: `npx vitest run`
Expected: suíte verde (nada quebrado; o job não roda em teste).

- [ ] **Step 6: Commit**

```bash
git add src/lib/queues.ts src/jobs/lgpd/ src/jobs/worker.ts
git commit -m "feat(lgpd): job BullMQ de expurgo diario (retencao 12 meses configuravel)"
```

---

### Task 3: Envs + verificação final

**Files:**
- Modify: `.env.example` (se existir)

- [ ] **Step 1: Documentar envs.** Se `.env.example` existir, adicionar:
```
# LGPD — retenção/expurgo
RETENCAO_LOGS_MESES=12
EXPURGO_INTERVAL_HOURS=24
```
Se NÃO existir, pular (não criar arquivo novo) e anotar no relatório.

- [ ] **Step 2:** `npx tsc --noEmit` → limpo.
- [ ] **Step 3:** `npx vitest run` → todos verdes (419 + novos).
- [ ] **Step 4:** `npx next lint` (arquivos tocados) → sem novos erros.
- [ ] **Step 5: Commit** (se `.env.example` mudou):
```bash
git add .env.example
git commit -m "docs: envs de retencao/expurgo no .env.example"
```
- [ ] **Step 6:** Depois: finishing-a-development-branch.

---

## Self-Review

- **Cobertura da spec:** serviço + corte 12 meses → T1; fila+job+scheduler → T2; envs+verificação → T3. ✔
- **Placeholders:** código completo em cada step; padrões espelhados de `nf-capture`/`queues.ts`/`worker.ts` (lidos). ✔
- **Consistência:** `expurgarDadosAntigos({retencaoLogsMeses})`/`ResultadoExpurgo` idênticos em T1/T2; nome de fila `'lgpd-expurgo'` consistente entre queue, worker e scheduler. ✔
