# LGPD G2c-2 — Ferramentas do operador (dados do cliente final) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) ou superpowers:executing-plans. Steps usam checkbox (`- [ ]`).

**Goal:** Permitir que o restaurante (operador) busque um cliente final por telefone e exporte ou exclua (hard-delete) os dados dele em Reserva, WhatsAppContato e WhatsAppLog.

**Architecture:** Serviço tenant-scoped faz busca (filtro por telefone normalizado em JS), export e exclusão (`deleteMany` por ids casados + tenantId); rotas finas (admin/MANAGER) expõem; UI em Configurações.

**Tech Stack:** Next.js 14 route handlers, Prisma, NextAuth (`getSession`), Vitest.

## Global Constraints

- Tudo **tenant-scoped**: nenhuma operação pode ler/excluir dados de outro tenant. `deleteMany` sempre com `tenantId` no where.
- Exclusão = **hard-delete** das 3 tabelas (Reserva, WhatsAppContato, WhatsAppLog). Não tocar iFood/endereço.
- Acesso: role ∈ {SUPER_ADMIN, ADMIN, MANAGER}.
- Telefone curto (< 4 dígitos normalizados) e sem nome → resultado vazio (evita casar tudo).
- Suíte verde; tsc/lint limpos.

---

### Task 1: Serviço `cliente-dados.service.ts`

**Files:**
- Create: `src/services/lgpd/cliente-dados.service.ts`
- Test: `src/services/lgpd/__tests__/cliente-dados.service.test.ts`

**Interfaces:**
- Produces: `normalizarTelefone(t: string): string`; `buscarDadosCliente(tenantId, telefone, nome?): Promise<DadosCliente>` onde `DadosCliente = { reservas: any[]; contatosWhatsapp: any[]; logsWhatsapp: any[] }`; `excluirDadosCliente(tenantId, telefone, nome?): Promise<{ reservas: number; contatos: number; logs: number }>`.

- [ ] **Step 1: Testes que falham.** Criar o teste:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
vi.mock('@/lib/prisma', () => ({ prisma: {
  reserva: { findMany: vi.fn(), deleteMany: vi.fn() },
  whatsAppContato: { findMany: vi.fn(), deleteMany: vi.fn() },
  whatsAppLog: { findMany: vi.fn(), deleteMany: vi.fn() },
} }))
import { normalizarTelefone, buscarDadosCliente, excluirDadosCliente } from '../cliente-dados.service'
import { prisma } from '@/lib/prisma'
const mp = prisma as any
beforeEach(() => vi.clearAllMocks())

describe('normalizarTelefone', () => {
  it('mantem so digitos', () => {
    expect(normalizarTelefone('(11) 99999-8888')).toBe('11999998888')
    expect(normalizarTelefone('+55 11 9 8888-7777')).toBe('5511988887777')
    expect(normalizarTelefone('')).toBe('')
  })
})

describe('buscarDadosCliente', () => {
  it('casa por telefone normalizado nas 3 fontes, ignora outros', async () => {
    mp.reserva.findMany.mockResolvedValue([
      { id: 'r1', clienteNome: 'Ana', contato: '(11) 99999-8888' },
      { id: 'r2', clienteNome: 'Bob', contato: '11 3333-2222' },
    ])
    mp.whatsAppContato.findMany.mockResolvedValue([{ id: 'c1', nome: 'Ana', numero: '5511999998888' }])
    mp.whatsAppLog.findMany.mockResolvedValue([
      { id: 'l1', destinatario: '5511999998888', conteudo: 'oi' },
      { id: 'l2', destinatario: '5511000000000', conteudo: 'x' },
    ])
    const r = await buscarDadosCliente('t1', '99999-8888')
    expect(r.reservas.map((x: any) => x.id)).toEqual(['r1'])
    expect(r.contatosWhatsapp.map((x: any) => x.id)).toEqual(['c1'])
    expect(r.logsWhatsapp.map((x: any) => x.id)).toEqual(['l1'])
    // tenant-scoped
    expect(mp.reserva.findMany.mock.calls[0][0].where).toEqual({ tenantId: 't1' })
  })

  it('telefone curto sem nome retorna vazio (nao casa tudo)', async () => {
    const r = await buscarDadosCliente('t1', '99')
    expect(r).toEqual({ reservas: [], contatosWhatsapp: [], logsWhatsapp: [] })
    expect(mp.reserva.findMany).not.toHaveBeenCalled()
  })
})

describe('excluirDadosCliente', () => {
  it('deleteMany escopado por ids casados + tenantId', async () => {
    mp.reserva.findMany.mockResolvedValue([{ id: 'r1', contato: '11999998888' }])
    mp.whatsAppContato.findMany.mockResolvedValue([{ id: 'c1', numero: '11999998888' }])
    mp.whatsAppLog.findMany.mockResolvedValue([])
    mp.reserva.deleteMany.mockResolvedValue({ count: 1 })
    mp.whatsAppContato.deleteMany.mockResolvedValue({ count: 1 })
    mp.whatsAppLog.deleteMany.mockResolvedValue({ count: 0 })
    const r = await excluirDadosCliente('t1', '11999998888')
    expect(r).toEqual({ reservas: 1, contatos: 1, logs: 0 })
    expect(mp.reserva.deleteMany).toHaveBeenCalledWith({ where: { id: { in: ['r1'] }, tenantId: 't1' } })
    expect(mp.whatsAppContato.deleteMany).toHaveBeenCalledWith({ where: { id: { in: ['c1'] }, tenantId: 't1' } })
    expect(mp.whatsAppLog.deleteMany).toHaveBeenCalledWith({ where: { id: { in: [] }, tenantId: 't1' } })
  })
})
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run src/services/lgpd/__tests__/cliente-dados.service.test.ts`
Expected: FAIL (módulo não existe).

- [ ] **Step 3: Implementar:**

```ts
import { prisma } from '@/lib/prisma'

export type DadosCliente = {
  reservas: unknown[]
  contatosWhatsapp: unknown[]
  logsWhatsapp: unknown[]
}

export function normalizarTelefone(t: string): string {
  return (t ?? '').replace(/\D/g, '')
}

export async function buscarDadosCliente(
  tenantId: string,
  telefone: string,
  nome?: string,
): Promise<DadosCliente> {
  const telNorm = normalizarTelefone(telefone)
  const nomeNorm = nome?.trim().toLowerCase()
  if (telNorm.length < 4 && !nomeNorm) {
    return { reservas: [], contatosWhatsapp: [], logsWhatsapp: [] }
  }

  const [reservas, contatos, logs] = await Promise.all([
    prisma.reserva.findMany({ where: { tenantId } }),
    prisma.whatsAppContato.findMany({ where: { tenantId } }),
    prisma.whatsAppLog.findMany({ where: { tenantId } }),
  ])

  const casaTel = (campo: string | null | undefined) =>
    telNorm.length >= 4 && normalizarTelefone(campo ?? '').includes(telNorm)

  return {
    reservas: (reservas as Array<{ contato: string | null; clienteNome: string | null }>).filter(
      (r) => casaTel(r.contato) || (!!nomeNorm && (r.clienteNome ?? '').toLowerCase().includes(nomeNorm)),
    ),
    contatosWhatsapp: (contatos as Array<{ numero: string }>).filter((c) => casaTel(c.numero)),
    logsWhatsapp: (logs as Array<{ destinatario: string }>).filter((l) => casaTel(l.destinatario)),
  }
}

export async function excluirDadosCliente(
  tenantId: string,
  telefone: string,
  nome?: string,
): Promise<{ reservas: number; contatos: number; logs: number }> {
  const achados = await buscarDadosCliente(tenantId, telefone, nome)
  const ids = (arr: unknown[]) => arr.map((x) => (x as { id: string }).id)

  const [r, c, l] = await Promise.all([
    prisma.reserva.deleteMany({ where: { id: { in: ids(achados.reservas) }, tenantId } }),
    prisma.whatsAppContato.deleteMany({ where: { id: { in: ids(achados.contatosWhatsapp) }, tenantId } }),
    prisma.whatsAppLog.deleteMany({ where: { id: { in: ids(achados.logsWhatsapp) }, tenantId } }),
  ])
  return { reservas: r.count, contatos: c.count, logs: l.count }
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run src/services/lgpd/__tests__/cliente-dados.service.test.ts`
Expected: PASS (todos). Depois `npx tsc --noEmit` limpo.

- [ ] **Step 5: Commit**

```bash
git add src/services/lgpd/cliente-dados.service.ts src/services/lgpd/__tests__/cliente-dados.service.test.ts
git commit -m "feat(lgpd): servico de busca/export/exclusao de dados do cliente final"
```

---

### Task 2: Rotas

**Files:**
- Create: `src/app/api/clientes/dados/route.ts` (GET preview)
- Create: `src/app/api/clientes/dados/exportar/route.ts` (GET download)
- Create: `src/app/api/clientes/dados/excluir/route.ts` (POST)
- Test: `src/app/api/clientes/dados/__tests__/excluir.route.test.ts`

**Interfaces:**
- Consumes: `buscarDadosCliente`, `exportarDadosCliente`(= reusa buscar), `excluirDadosCliente`; `getSession`/`unauthorizedResponse`.

- [ ] **Step 1: `dados/route.ts` (preview):**

```ts
import { NextRequest, NextResponse } from 'next/server'
import { getSession, unauthorizedResponse } from '@/lib/session'
import { buscarDadosCliente } from '@/services/lgpd/cliente-dados.service'

function permitido(role?: string | null) {
  return role === 'SUPER_ADMIN' || role === 'ADMIN' || role === 'MANAGER'
}

export async function GET(req: NextRequest) {
  const session = await getSession()
  if (!session?.user?.tenantId) return unauthorizedResponse()
  if (!permitido(session.user.role)) return NextResponse.json({ error: 'Sem permissão' }, { status: 403 })
  const { searchParams } = new URL(req.url)
  const telefone = searchParams.get('telefone') ?? ''
  const nome = searchParams.get('nome') ?? undefined
  const dados = await buscarDadosCliente(session.user.tenantId, telefone, nome)
  return NextResponse.json(dados)
}
```

- [ ] **Step 2: `dados/exportar/route.ts`:**

```ts
import { NextRequest, NextResponse } from 'next/server'
import { getSession, unauthorizedResponse } from '@/lib/session'
import { buscarDadosCliente } from '@/services/lgpd/cliente-dados.service'

function permitido(role?: string | null) {
  return role === 'SUPER_ADMIN' || role === 'ADMIN' || role === 'MANAGER'
}

export async function GET(req: NextRequest) {
  const session = await getSession()
  if (!session?.user?.tenantId) return unauthorizedResponse()
  if (!permitido(session.user.role)) return NextResponse.json({ error: 'Sem permissão' }, { status: 403 })
  const { searchParams } = new URL(req.url)
  const dados = await buscarDadosCliente(session.user.tenantId, searchParams.get('telefone') ?? '', searchParams.get('nome') ?? undefined)
  return new NextResponse(JSON.stringify(dados, null, 2), {
    status: 200,
    headers: { 'Content-Type': 'application/json', 'Content-Disposition': 'attachment; filename="dados-cliente.json"' },
  })
}
```

- [ ] **Step 3: Teste de `excluir` que falha:**

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
vi.mock('@/lib/session', () => ({ getSession: vi.fn(), unauthorizedResponse: () => new Response(JSON.stringify({ error: 'Não autorizado' }), { status: 401 }) }))
vi.mock('@/services/lgpd/cliente-dados.service', () => ({ excluirDadosCliente: vi.fn() }))
import { POST } from '../excluir/route'
import { getSession } from '@/lib/session'
import { excluirDadosCliente } from '@/services/lgpd/cliente-dados.service'
beforeEach(() => vi.clearAllMocks())

function reqWith(body: any) {
  return new Request('http://x/api/clientes/dados/excluir', { method: 'POST', body: JSON.stringify(body) }) as any
}

it('403 para nao-admin', async () => {
  ;(getSession as any).mockResolvedValue({ user: { tenantId: 't1', role: 'STAFF' } })
  const res = await POST(reqWith({ telefone: '11999998888' }))
  expect(res.status).toBe(403)
  expect(excluirDadosCliente).not.toHaveBeenCalled()
})

it('200 + contagens para admin', async () => {
  ;(getSession as any).mockResolvedValue({ user: { tenantId: 't1', role: 'ADMIN' } })
  ;(excluirDadosCliente as any).mockResolvedValue({ reservas: 2, contatos: 1, logs: 5 })
  const res = await POST(reqWith({ telefone: '11999998888' }))
  expect(res.status).toBe(200)
  expect(await res.json()).toEqual({ reservas: 2, contatos: 1, logs: 5 })
  expect(excluirDadosCliente).toHaveBeenCalledWith('t1', '11999998888', undefined)
})
```

- [ ] **Step 4: Rodar e ver falhar**

Run: `npx vitest run src/app/api/clientes/dados/__tests__/excluir.route.test.ts`
Expected: FAIL (rota não existe).

- [ ] **Step 5: `dados/excluir/route.ts`:**

```ts
import { NextRequest, NextResponse } from 'next/server'
import { getSession, unauthorizedResponse } from '@/lib/session'
import { excluirDadosCliente } from '@/services/lgpd/cliente-dados.service'

function permitido(role?: string | null) {
  return role === 'SUPER_ADMIN' || role === 'ADMIN' || role === 'MANAGER'
}

export async function POST(req: NextRequest) {
  const session = await getSession()
  if (!session?.user?.tenantId) return unauthorizedResponse()
  if (!permitido(session.user.role)) return NextResponse.json({ error: 'Sem permissão' }, { status: 403 })
  const body = await req.json().catch(() => ({}))
  const telefone: string = body?.telefone ?? ''
  const nome: string | undefined = body?.nome ?? undefined
  if (!telefone && !nome) return NextResponse.json({ error: 'Informe um telefone.' }, { status: 400 })
  const contagens = await excluirDadosCliente(session.user.tenantId, telefone, nome)
  return NextResponse.json(contagens)
}
```

- [ ] **Step 6: Rodar e ver passar**

Run: `npx vitest run src/app/api/clientes/dados/__tests__/excluir.route.test.ts`
Expected: PASS. Depois `npx tsc --noEmit` limpo.

- [ ] **Step 7: Commit**

```bash
git add src/app/api/clientes/dados/
git commit -m "feat(lgpd): rotas de busca/export/exclusao de dados do cliente final (admin/MANAGER)"
```

---

### Task 3: UI + link na sidebar

**Files:**
- Create: `src/app/(dashboard)/configuracoes/dados-clientes/page.tsx`
- Modify: `src/components/layout/sidebar.tsx`

- [ ] **Step 1: Página (client component)** seguindo o estilo de `configuracoes/privacidade/page.tsx` (LER primeiro):
  - Header "Dados de Clientes (LGPD)" + subtítulo curto explicando a finalidade (atender pedido de titular).
  - Form: input **telefone** (+ nome opcional) + botão "Buscar" → `GET /api/clientes/dados?telefone=...&nome=...` → guarda o resultado.
  - Preview: 3 seções (Reservas, Contatos WhatsApp, Mensagens WhatsApp) listando os registros achados; contadores; mensagem de vazio.
  - Botões (habilitados se houver resultado): **"Exportar (JSON)"** → abre/baixa `GET /api/clientes/dados/exportar?...` (via blob); **"Excluir dados"** → confirmação digitando `EXCLUIR` → `POST /api/clientes/dados/excluir` → mostra contagens apagadas e limpa o resultado.
  - Estados loading/erro.

- [ ] **Step 2: Link na sidebar.** Em `src/components/layout/sidebar.tsx`, no grupo "Configurações" (array `children`), adicionar `{ label: 'Dados de Clientes', href: '/configuracoes/dados-clientes', icon: <um ícone lucide já importado, ex.: Users ou ShieldCheck — se novo, adicionar ao import> }`. Seguir o formato exato das outras entradas.

- [ ] **Step 3: Verificar**

Run: `npx tsc --noEmit` (limpo) e `npx next lint --file "src/app/(dashboard)/configuracoes/dados-clientes/page.tsx" --file "src/components/layout/sidebar.tsx"` (sem novos erros).

- [ ] **Step 4: Commit**

```bash
git add "src/app/(dashboard)/configuracoes/dados-clientes/page.tsx" src/components/layout/sidebar.tsx
git commit -m "feat(lgpd): UI Dados de Clientes (buscar/exportar/excluir) + link na sidebar"
```

---

### Task 4: Verificação final

- [ ] **Step 1:** `npx tsc --noEmit` → limpo.
- [ ] **Step 2:** `npx vitest run` → todos verdes (421 + novos).
- [ ] **Step 3:** `npx next lint` (arquivos tocados) → sem novos erros.
- [ ] **Step 4:** Depois: finishing-a-development-branch.

---

## Self-Review

- **Cobertura da spec:** serviço busca/export/exclusão tenant-scoped → T1; rotas + guard → T2; UI + sidebar → T3; verificação → T4. ✔
- **Placeholders:** código completo no serviço e rotas; UI descrita por comportamento concreto + seguir `privacidade/page.tsx`. ✔
- **Consistência:** `buscarDadosCliente`/`excluirDadosCliente` assinaturas idênticas em T1/T2; `DadosCliente` e o retorno `{reservas,contatos,logs}` consistentes; rotas todas com guard `permitido`. Export reusa `buscarDadosCliente` (sem função `exportarDadosCliente` separada — a rota serializa o resultado da busca). ✔
